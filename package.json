{
  "name": "hospedah",
  "version": "1.0.0",
  "description": "HOSPEDAH — Resorts e Experiências Exclusivas",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "serve": "npx serve . -p 4000 -s"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
