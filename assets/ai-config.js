// HOSPEDAH — AI configuration placeholder
// A chave da API Gemini é gerenciada exclusivamente no servidor (Supabase Edge Function).
// Não defina window.GEMINI_API_KEY aqui — todas as chamadas passam pela Edge Function.
